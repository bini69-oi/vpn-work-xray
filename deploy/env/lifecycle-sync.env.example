# Optional overrides for lifecycle_daily_sync.sh
# ENV_FILE=/etc/vpn-product/vpn-productd.env
# PDB=/var/lib/vpn-product/product.db
# API_URL=http://127.0.0.1:8080
# RENEW_DAYS=30
# Comma-separated users that should auto-renew instead of being blocked after expiry.
# AUTO_RENEW_USERS=owner_main
